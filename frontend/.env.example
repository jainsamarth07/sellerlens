VITE_API_BASE_URL=/api

# Microsoft Entra ID (optional, only needed if using the MSAL browser SDK
# directly. The default flow goes through the backend so these can be blank.)
VITE_MICROSOFT_CLIENT_ID=
VITE_REDIRECT_URI=http://localhost:5173/auth/callback
